<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Detail</title>
    <link href="/styles.css" rel="stylesheet" />
  </head>

  <body class="grey-bg">
    <% if (!user) { %> <%- include("nav.ejs") %> <% } %>
    <span></span>
    <% if (user) { %> <%- include("navi.ejs") %> <% } %>

    <div class="detail-bg">
      <h4><%= postData.title %></h4>
      <p><%= postData.content %></p>
      <img src="<%= postData.img %>" onerror="this.style.display='none'" />

      <a href="/chat/request?postUser=<%= postData.user %>">Start Chatting</a>

      <hr style="margin-top: 60px" />
      <div>
        <% for (let i = 0; i < commentData.length; i++) { %>
        <p>
          <strong><%= commentData[i].username %>:</strong>
          <%= commentData[i].content %>
        </p>
        <% } %>
      </div>

      <form action="/comment" method="POST">
        <input name="content" />
        <input
          name="parentId"
          value="<%= postData._id %>"
          style="display: none"
        />
        <button type="submit">Add a comment</button>
      </form>
    </div>
  </body>
</html>
